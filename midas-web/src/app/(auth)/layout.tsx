/**
 * Auth Layout - Midas Design System
 * 
 * Figma Reference: Node 639-1976, 639-2096
 * Simple passthrough - auth pages handle their own full-screen layouts.
 */

export default function AuthLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
